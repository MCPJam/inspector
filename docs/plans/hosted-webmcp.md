# Hosted WebMCP entry point

Hosted users should see WebMCP in the sidebar. Selecting it should present the
Node.js installation and desktop download options because browser inspection
requires a local MCPJam instance. Local and desktop WebMCP behavior should remain
unchanged.

Validation covers hosted navigation visibility, install/download destinations,
and local WebMCP access. Capture screenshots and a recording before review.
