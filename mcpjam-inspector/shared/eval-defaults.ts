/**
 * Iterations a freshly authored case runs per model. Shared so the client
 * editor, staged Markdown imports, and the server's Markdown import route all
 * mint cases with the same default.
 */
export const DEFAULT_RUNS_PER_TEST = 5;
