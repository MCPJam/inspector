/**
 * The production deploy workflow replaces this value immediately before
 * uploading the webapp to Railway. Local, desktop, and non-production builds
 * keep the catalog's MCPJam verification date.
 */
export const MCPJAM_WEB_DEPLOYED_AT: number | null = null;
