/**
 * Replaced by `.github/scripts/stamp-mcpjam-build-time.mjs` in every release
 * build that ships a client bundle to users: the webapp deploy to Railway, the
 * `@mcpjam/inspector` tarball behind `npx`, and the macOS and Windows desktop
 * apps. Local and non-production builds keep the catalog's MCPJam
 * verification date.
 */
export const MCPJAM_WEB_DEPLOYED_AT: number | null = null;
