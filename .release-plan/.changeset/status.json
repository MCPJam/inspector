{
  "changesets": [
    {
      "releases": [
        {
          "name": "@mcpjam/inspector",
          "type": "minor"
        },
        {
          "name": "@mcpjam/cli",
          "type": "minor"
        }
      ],
      "summary": "New `mcpjam apps session start|action|close` drives an MCP App widget interactively, headlessly: `start` renders a widget and keeps it mounted (a live Chromium tab), `action` steps it with a Computer-Use action (click/type/scroll/…) and reports the post-action frame plus any widget→host `tools/call`s it triggered, and `close` disposes it. The CLI exposes the session; the external agent drives the steps (no LLM is embedded).\n\nBacked by a new local-only Inspector surface (`POST /api/mcp/widget-session`, `POST /api/mcp/widget-session/:id/action`, `DELETE /api/mcp/widget-session/:id`) over a session registry with strict browser lifecycle — max concurrent sessions cap, idle TTL refreshed on each action, a periodic sweep, and dispose-all on process shutdown — so a mounted tab can't leak. The gate-first render flow (listTools → renderability gate → executeTool → render) is shared with `apps render` via a common core. Same output conventions as `apps render`: screenshot to a file by default (`--screenshot-out`), inline base64 opt-in (`--screenshot-base64`).",
      "id": "cli-apps-session"
    },
    {
      "releases": [
        {
          "name": "@mcpjam/sdk",
          "type": "minor"
        },
        {
          "name": "@mcpjam/cli",
          "type": "minor"
        },
        {
          "name": "@mcpjam/inspector",
          "type": "minor"
        }
      ],
      "summary": "Add `create_eval_suite`: author runnable eval suites from the CLI and MCP.\n\n- **@mcpjam/sdk**: new `createEvalSuiteOperation` platform operation plus\n  `PlatformApiClient.createEvalSuite` and the `PlatformEvalSuiteCreated` type.\n  Resolves the project selector and HTTP-only servers like `run_eval_suite`,\n  and forwards the full case shape (expected tool calls, expected output,\n  advanced config, prompt turns, match options, predicates, widget probes).\n- **@mcpjam/cli**: new `mcpjam eval` command group (`create` / `list` / `run` /\n  `status`). `create` accepts a JSON suite definition via `--file`/`--json`\n  with `--name`/`--model`/`--provider`/`--server`/`--project` overrides and\n  authors a suite without running it.\n- **@mcpjam/inspector**: new author-only `POST /api/v1/projects/:projectId/eval-suites`\n  endpoint that creates a runnable suite from test-case definitions (extracted\n  from the eval-run path, which is unchanged). The v1 guest gate is now\n  method-aware: `GET /eval-suites` stays guest-allowed, `POST` is denied.",
      "id": "create-eval-suite"
    },
    {
      "releases": [
        {
          "name": "@mcpjam/inspector",
          "type": "minor"
        }
      ],
      "summary": "Route brand-new signups to the Playground on first load while keeping returning users on Home. Onboarding state now distinguishes a first-ever session from a returning one so the initial destination matches the user's intent.",
      "id": "nux-signup-routing"
    },
    {
      "releases": [
        {
          "name": "@mcpjam/inspector",
          "type": "patch"
        }
      ],
      "summary": "Fix the sidebar flashing a signed-out layout while authentication is still resolving. The sidebar and user section now hold their rendering until auth state settles, avoiding a brief flicker on load.",
      "id": "sidebar-auth-flash"
    }
  ],
  "releases": [
    {
      "name": "@mcpjam/inspector",
      "type": "minor",
      "oldVersion": "2.13.0",
      "changesets": [
        "cli-apps-session",
        "create-eval-suite",
        "nux-signup-routing",
        "sidebar-auth-flash"
      ],
      "newVersion": "2.14.0"
    },
    {
      "name": "@mcpjam/cli",
      "type": "minor",
      "oldVersion": "3.7.0",
      "changesets": [
        "cli-apps-session",
        "create-eval-suite"
      ],
      "newVersion": "3.8.0"
    },
    {
      "name": "@mcpjam/sdk",
      "type": "minor",
      "oldVersion": "1.18.0",
      "changesets": [
        "create-eval-suite"
      ],
      "newVersion": "1.19.0"
    },
    {
      "name": "@mcpjam/mcp",
      "type": "none",
      "oldVersion": "0.0.1",
      "changesets": [],
      "newVersion": "0.0.1"
    }
  ]
}