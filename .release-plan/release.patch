diff --git a/.changeset/cli-apps-session.md b/.changeset/cli-apps-session.md
deleted file mode 100644
index ca9715d94..000000000
--- a/.changeset/cli-apps-session.md
+++ /dev/null
@@ -1,8 +0,0 @@
----
-"@mcpjam/inspector": minor
-"@mcpjam/cli": minor
----
-
-New `mcpjam apps session start|action|close` drives an MCP App widget interactively, headlessly: `start` renders a widget and keeps it mounted (a live Chromium tab), `action` steps it with a Computer-Use action (click/type/scroll/…) and reports the post-action frame plus any widget→host `tools/call`s it triggered, and `close` disposes it. The CLI exposes the session; the external agent drives the steps (no LLM is embedded).
-
-Backed by a new local-only Inspector surface (`POST /api/mcp/widget-session`, `POST /api/mcp/widget-session/:id/action`, `DELETE /api/mcp/widget-session/:id`) over a session registry with strict browser lifecycle — max concurrent sessions cap, idle TTL refreshed on each action, a periodic sweep, and dispose-all on process shutdown — so a mounted tab can't leak. The gate-first render flow (listTools → renderability gate → executeTool → render) is shared with `apps render` via a common core. Same output conventions as `apps render`: screenshot to a file by default (`--screenshot-out`), inline base64 opt-in (`--screenshot-base64`).
diff --git a/.changeset/create-eval-suite.md b/.changeset/create-eval-suite.md
deleted file mode 100644
index cb41b0a8b..000000000
--- a/.changeset/create-eval-suite.md
+++ /dev/null
@@ -1,21 +0,0 @@
----
-"@mcpjam/sdk": minor
-"@mcpjam/cli": minor
-"@mcpjam/inspector": minor
----
-
-Add `create_eval_suite`: author runnable eval suites from the CLI and MCP.
-
-- **@mcpjam/sdk**: new `createEvalSuiteOperation` platform operation plus
-  `PlatformApiClient.createEvalSuite` and the `PlatformEvalSuiteCreated` type.
-  Resolves the project selector and HTTP-only servers like `run_eval_suite`,
-  and forwards the full case shape (expected tool calls, expected output,
-  advanced config, prompt turns, match options, predicates, widget probes).
-- **@mcpjam/cli**: new `mcpjam eval` command group (`create` / `list` / `run` /
-  `status`). `create` accepts a JSON suite definition via `--file`/`--json`
-  with `--name`/`--model`/`--provider`/`--server`/`--project` overrides and
-  authors a suite without running it.
-- **@mcpjam/inspector**: new author-only `POST /api/v1/projects/:projectId/eval-suites`
-  endpoint that creates a runnable suite from test-case definitions (extracted
-  from the eval-run path, which is unchanged). The v1 guest gate is now
-  method-aware: `GET /eval-suites` stays guest-allowed, `POST` is denied.
diff --git a/.changeset/nux-signup-routing.md b/.changeset/nux-signup-routing.md
deleted file mode 100644
index 6e1991183..000000000
--- a/.changeset/nux-signup-routing.md
+++ /dev/null
@@ -1,5 +0,0 @@
----
-"@mcpjam/inspector": minor
----
-
-Route brand-new signups to the Playground on first load while keeping returning users on Home. Onboarding state now distinguishes a first-ever session from a returning one so the initial destination matches the user's intent.
diff --git a/.changeset/sidebar-auth-flash.md b/.changeset/sidebar-auth-flash.md
deleted file mode 100644
index 0859f5391..000000000
--- a/.changeset/sidebar-auth-flash.md
+++ /dev/null
@@ -1,5 +0,0 @@
----
-"@mcpjam/inspector": patch
----
-
-Fix the sidebar flashing a signed-out layout while authentication is still resolving. The sidebar and user section now hold their rendering until auth state settles, avoiding a brief flicker on load.
diff --git a/cli/package.json b/cli/package.json
index fa23a15f9..d563cba8d 100644
--- a/cli/package.json
+++ b/cli/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@mcpjam/cli",
-  "version": "3.7.0",
+  "version": "3.8.0",
   "description": "Test, debug, and validate MCP servers — health checks, OAuth conformance, tool-surface diffing, and structured triage from the terminal or CI",
   "type": "module",
   "main": "dist/index.js",
@@ -17,7 +17,7 @@
     "prepublishOnly": "npm run build"
   },
   "dependencies": {
-    "@mcpjam/sdk": "^1.18.0",
+    "@mcpjam/sdk": "^1.19.0",
     "@modelcontextprotocol/server": "^2.0.0-alpha.2",
     "commander": "^12.1.0",
     "posthog-node": "^5.24.10",
diff --git a/mcpjam-inspector/package.json b/mcpjam-inspector/package.json
index ac4a51cbc..d73da8ce7 100644
--- a/mcpjam-inspector/package.json
+++ b/mcpjam-inspector/package.json
@@ -1,7 +1,7 @@
 {
   "name": "@mcpjam/inspector",
   "productName": "MCPJam Inspector",
-  "version": "2.13.0",
+  "version": "2.14.0",
   "type": "module",
   "engines": {
     "node": ">=22.0.0"
@@ -127,7 +127,7 @@
     "@hookform/resolvers": "^3.10.0",
     "@lezer/highlight": "^1.2.3",
     "@mcp-ui/client": "^5.9.0",
-    "@mcpjam/sdk": "^1.18.0",
+    "@mcpjam/sdk": "^1.19.0",
     "@modelcontextprotocol/client": "^2.0.0-alpha.2",
     "@modelcontextprotocol/ext-apps": "^1.7.2",
     "@openrouter/ai-sdk-provider": "^2.0.2",
diff --git a/package-lock.json b/package-lock.json
index 9bef76c35..8cdb3330e 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -58,9 +58,9 @@
     },
     "cli": {
       "name": "@mcpjam/cli",
-      "version": "3.7.0",
+      "version": "3.8.0",
       "dependencies": {
-        "@mcpjam/sdk": "^1.18.0",
+        "@mcpjam/sdk": "^1.19.0",
         "@modelcontextprotocol/server": "^2.0.0-alpha.2",
         "commander": "^12.1.0",
         "posthog-node": "^5.24.10",
@@ -327,7 +327,7 @@
     },
     "mcpjam-inspector": {
       "name": "@mcpjam/inspector",
-      "version": "2.13.0",
+      "version": "2.14.0",
       "license": "Apache-2.0",
       "dependencies": {
         "@ai-sdk/amazon-bedrock": "^4.0.116",
@@ -357,7 +357,7 @@
         "@hookform/resolvers": "^3.10.0",
         "@lezer/highlight": "^1.2.3",
         "@mcp-ui/client": "^5.9.0",
-        "@mcpjam/sdk": "^1.18.0",
+        "@mcpjam/sdk": "^1.19.0",
         "@modelcontextprotocol/client": "^2.0.0-alpha.2",
         "@modelcontextprotocol/ext-apps": "^1.7.2",
         "@openrouter/ai-sdk-provider": "^2.0.2",
@@ -36585,7 +36585,7 @@
     },
     "sdk": {
       "name": "@mcpjam/sdk",
-      "version": "1.18.0",
+      "version": "1.19.0",
       "license": "MIT",
       "dependencies": {
         "@ai-sdk/amazon-bedrock": "^3.0.101",
diff --git a/sdk/package.json b/sdk/package.json
index 4b9bca356..7fafd3546 100644
--- a/sdk/package.json
+++ b/sdk/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@mcpjam/sdk",
-  "version": "1.18.0",
+  "version": "1.19.0",
   "description": "MCP server unit testing, end to end (e2e) testing, and server evals",
   "type": "module",
   "main": "dist/index.js",
