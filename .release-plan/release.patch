diff --git a/.changeset/scorecard-stage-rail.md b/.changeset/scorecard-stage-rail.md
deleted file mode 100644
index 95c909295..000000000
--- a/.changeset/scorecard-stage-rail.md
+++ /dev/null
@@ -1,5 +0,0 @@
----
-"@mcpjam/inspector": patch
----
-
-Bring the stage rail back to the iteration scorecard. The six links of the user-value chain read as a column again, with one stage open beside it, and the open stage keeps the EXPECTED / ACTUAL rows the flat layout added. It opens on the stage the run broke at, on User value while a reviewer is labelling blind, and on the end of the chain when nothing broke. A masked stage's dot stays neutral, and an iteration with no verified chain stacks its sections as before.
diff --git a/.changeset/sdk-refresh-token-rotation-hook.md b/.changeset/sdk-refresh-token-rotation-hook.md
deleted file mode 100644
index 1088344d3..000000000
--- a/.changeset/sdk-refresh-token-rotation-hook.md
+++ /dev/null
@@ -1,11 +0,0 @@
----
-"@mcpjam/sdk": minor
----
-
-Report a rotated OAuth refresh token to the caller, via a new `onTokensRotated` option on an HTTP server config.
-
-Most authorization servers issue single-use refresh tokens, so the value passed as `refreshToken` is spent once it has been exchanged. The SDK kept the replacement in memory, which is invisible for the life of a connection and fatal beyond it: a CI job or any other long-lived caller configured from a secret authorized once and then failed, with nothing to say a credential had been silently replaced.
-
-`onTokensRotated` receives the replacement so it can be persisted back to wherever the original came from. It fires only when the token actually changed, and it is awaited before the connection completes, so a job that exits as soon as it is done still gets the write. A handler that throws or rejects never fails a connection that has already authorized.
-
-The documented behaviour in `docs/sdk/concepts/connecting-servers.mdx` was also corrected: it claimed the SDK "stores rotated refresh tokens" without saying that the store dies with the process.
diff --git a/mcpjam-inspector/package.json b/mcpjam-inspector/package.json
index b0a03bc67..0b5bc648e 100644
--- a/mcpjam-inspector/package.json
+++ b/mcpjam-inspector/package.json
@@ -1,7 +1,7 @@
 {
   "name": "@mcpjam/inspector",
   "productName": "MCPJam Inspector",
-  "version": "3.9.0",
+  "version": "3.9.1",
   "type": "module",
   "engines": {
     "node": ">=22.0.0"
@@ -147,7 +147,7 @@
     "@hookform/resolvers": "^3.10.0",
     "@lezer/highlight": "^1.2.3",
     "@mcp-ui/client": "^5.9.0",
-    "@mcpjam/sdk": "^8.12.0",
+    "@mcpjam/sdk": "^8.13.0",
     "@modelcontextprotocol/client": "2.0.0",
     "@modelcontextprotocol/ext-apps": "^1.7.4",
     "@novnc/novnc": "^1.7.0",
diff --git a/package-lock.json b/package-lock.json
index ef99a69a6..ce9bfb857 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -426,7 +426,7 @@
     },
     "mcpjam-inspector": {
       "name": "@mcpjam/inspector",
-      "version": "3.9.0",
+      "version": "3.9.1",
       "license": "Apache-2.0",
       "dependencies": {
         "@ai-sdk/amazon-bedrock": "^4.0.116",
@@ -461,7 +461,7 @@
         "@hookform/resolvers": "^3.10.0",
         "@lezer/highlight": "^1.2.3",
         "@mcp-ui/client": "^5.9.0",
-        "@mcpjam/sdk": "^8.12.0",
+        "@mcpjam/sdk": "^8.13.0",
         "@modelcontextprotocol/client": "2.0.0",
         "@modelcontextprotocol/ext-apps": "^1.7.4",
         "@novnc/novnc": "^1.7.0",
@@ -39230,7 +39230,7 @@
     },
     "sdk": {
       "name": "@mcpjam/sdk",
-      "version": "8.12.0",
+      "version": "8.13.0",
       "license": "MIT",
       "dependencies": {
         "@ai-sdk/amazon-bedrock": "^3.0.101",
diff --git a/sdk/CHANGELOG.md b/sdk/CHANGELOG.md
index 56b2bb9eb..afd7f325b 100644
--- a/sdk/CHANGELOG.md
+++ b/sdk/CHANGELOG.md
@@ -1,5 +1,17 @@
 # `@mcpjam/sdk` changelog
 
+## 8.13.0
+
+### Minor Changes
+
+- [#5407](https://github.com/MCPJam/inspector/pull/5407) [`b0a1452`](https://github.com/MCPJam/inspector/commit/b0a1452e312b2f437012bddb728740e71db2a24c) Thanks [@chelojimenez](https://github.com/chelojimenez)! - Report a rotated OAuth refresh token to the caller, via a new `onTokensRotated` option on an HTTP server config.
+
+  Most authorization servers issue single-use refresh tokens, so the value passed as `refreshToken` is spent once it has been exchanged. The SDK kept the replacement in memory, which is invisible for the life of a connection and fatal beyond it: a CI job or any other long-lived caller configured from a secret authorized once and then failed, with nothing to say a credential had been silently replaced.
+
+  `onTokensRotated` receives the replacement so it can be persisted back to wherever the original came from. It fires only when the token actually changed, and it is awaited before the connection completes, so a job that exits as soon as it is done still gets the write. A handler that throws or rejects never fails a connection that has already authorized.
+
+  The documented behaviour in `docs/sdk/concepts/connecting-servers.mdx` was also corrected: it claimed the SDK "stores rotated refresh tokens" without saying that the store dies with the process.
+
 ## 8.12.0
 
 ### Minor Changes
diff --git a/sdk/package.json b/sdk/package.json
index f6906240d..9a1fb4c9b 100644
--- a/sdk/package.json
+++ b/sdk/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@mcpjam/sdk",
-  "version": "8.12.0",
+  "version": "8.13.0",
   "description": "MCP server unit testing, end to end (e2e) testing, and server evals",
   "type": "module",
   "main": "dist/index.js",
