diff --git a/.changeset/platform-refusal-guidance.md b/.changeset/platform-refusal-guidance.md
deleted file mode 100644
index 499eed21f..000000000
--- a/.changeset/platform-refusal-guidance.md
+++ /dev/null
@@ -1,7 +0,0 @@
----
-"@mcpjam/sdk": minor
-"@mcpjam/cli": patch
-"@mcpjam/inspector": patch
----
-
-Tell agents and scripts when an included operation's usage limit lifts. The SDK adds `describePlatformRefusal` and `platformRefusalHint`, which read a `RATE_LIMITED` refusal's backend code, limit, retry time and whether credits would help. MCP tool errors, CLI errors and in-app agent tool errors now carry that and say when to retry, without suggesting a top-up. Generation copy now says the quota belongs to the organization, not the project, and description proposals no longer claim a generation quota. The insight getters explain `platform_cap_exceeded` and `platform_unavailable`.
diff --git a/.changeset/sdk-empty-system-prompt.md b/.changeset/sdk-empty-system-prompt.md
deleted file mode 100644
index 3c12ab69e..000000000
--- a/.changeset/sdk-empty-system-prompt.md
+++ /dev/null
@@ -1,5 +0,0 @@
----
-"@mcpjam/sdk": patch
----
-
-Stop an empty system prompt from failing every generation. A saved MCPJam client with no system prompt reads as `""`, and `runWithClient` passed it straight to the provider; Anthropic refuses an empty system block with `system: text content blocks must be non-empty`, so every case in the suite failed with a bare "Bad Request". `HostRunner` now treats an empty configured prompt as "none given" and uses its default, the same as it already did for a host snapshot.
diff --git a/.changeset/swarm-evaluator-terms.md b/.changeset/swarm-evaluator-terms.md
deleted file mode 100644
index e6862a3f3..000000000
--- a/.changeset/swarm-evaluator-terms.md
+++ /dev/null
@@ -1,5 +0,0 @@
----
-"@mcpjam/inspector": patch
----
-
-Swarm screens use the evaluator vocabulary evals already use, and drop the advisory tier swarms no longer have. The goal grading editor, session chips, run scorecard, report panel, running step and Findings now say "evaluator" instead of "check" or "rubric check". The editor says every evaluator must pass for a session to pass, and the judge blurb says its verdict decides the session. Findings no longer renders "advisory friction" rows.
diff --git a/.changeset/swarm-persona-turn-step-cap.md b/.changeset/swarm-persona-turn-step-cap.md
deleted file mode 100644
index 2ce8bd17b..000000000
--- a/.changeset/swarm-persona-turn-step-cap.md
+++ /dev/null
@@ -1,5 +0,0 @@
----
-"@mcpjam/inspector": patch
----
-
-Cap swarm persona turns at 10 tool steps instead of inheriting the Playground's 30. Every step of a turn resends every tool result the turn has produced, so the cap bounds turn time and tokens together; on dev, uncapped persona turns against the MCPJam server reached 677k to 881k tokens and tripped the 6-minute turn budget. Scenario (User Testing) sessions keep the engine default.
diff --git a/.changeset/swarm-reporting-e2e.md b/.changeset/swarm-reporting-e2e.md
deleted file mode 100644
index 53bd7ad8c..000000000
--- a/.changeset/swarm-reporting-e2e.md
+++ /dev/null
@@ -1,7 +0,0 @@
----
-"@mcpjam/sdk": minor
-"@mcpjam/cli": minor
-"@mcpjam/inspector": patch
----
-
-Report swarm execution, goal grading, and advisory standard checks separately across the UI and platform API. Show shared run decisions, grading coverage, and session user-value chains while retaining persona-specific findings.
diff --git a/.changeset/wise-judge-findings.md b/.changeset/wise-judge-findings.md
deleted file mode 100644
index c646d331e..000000000
--- a/.changeset/wise-judge-findings.md
+++ /dev/null
@@ -1,5 +0,0 @@
----
-"@mcpjam/inspector": patch
----
-
-Show a muted explanation when automatic findings analysis fails, while keeping recorded findings visible.
diff --git a/cli/package.json b/cli/package.json
index f3bacaec5..c70fd0bc2 100644
--- a/cli/package.json
+++ b/cli/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@mcpjam/cli",
-  "version": "5.9.0",
+  "version": "5.10.0",
   "description": "Test, debug, and validate MCP servers — health checks, OAuth conformance, tool-surface diffing, and structured triage from the terminal or CI",
   "type": "module",
   "main": "dist/index.js",
@@ -20,7 +20,7 @@
     "prepublishOnly": "npm run build"
   },
   "dependencies": {
-    "@mcpjam/sdk": "^8.9.0",
+    "@mcpjam/sdk": "^8.10.0",
     "@modelcontextprotocol/server": "2.0.0",
     "commander": "^14.0.3",
     "posthog-node": "^5.24.10",
diff --git a/mcpjam-inspector/package.json b/mcpjam-inspector/package.json
index cde2e36ce..79268506a 100644
--- a/mcpjam-inspector/package.json
+++ b/mcpjam-inspector/package.json
@@ -1,7 +1,7 @@
 {
   "name": "@mcpjam/inspector",
   "productName": "MCPJam Inspector",
-  "version": "3.7.0",
+  "version": "3.7.1",
   "type": "module",
   "engines": {
     "node": ">=22.0.0"
@@ -147,7 +147,7 @@
     "@hookform/resolvers": "^3.10.0",
     "@lezer/highlight": "^1.2.3",
     "@mcp-ui/client": "^5.9.0",
-    "@mcpjam/sdk": "^8.9.0",
+    "@mcpjam/sdk": "^8.10.0",
     "@modelcontextprotocol/client": "2.0.0",
     "@modelcontextprotocol/ext-apps": "^1.7.4",
     "@novnc/novnc": "^1.7.0",
diff --git a/package-lock.json b/package-lock.json
index 629832fca..3b72a3528 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -64,9 +64,9 @@
     },
     "cli": {
       "name": "@mcpjam/cli",
-      "version": "5.9.0",
+      "version": "5.10.0",
       "dependencies": {
-        "@mcpjam/sdk": "^8.9.0",
+        "@mcpjam/sdk": "^8.10.0",
         "@modelcontextprotocol/server": "2.0.0",
         "commander": "^14.0.3",
         "posthog-node": "^5.24.10",
@@ -426,7 +426,7 @@
     },
     "mcpjam-inspector": {
       "name": "@mcpjam/inspector",
-      "version": "3.7.0",
+      "version": "3.7.1",
       "license": "Apache-2.0",
       "dependencies": {
         "@ai-sdk/amazon-bedrock": "^4.0.116",
@@ -461,7 +461,7 @@
         "@hookform/resolvers": "^3.10.0",
         "@lezer/highlight": "^1.2.3",
         "@mcp-ui/client": "^5.9.0",
-        "@mcpjam/sdk": "^8.9.0",
+        "@mcpjam/sdk": "^8.10.0",
         "@modelcontextprotocol/client": "2.0.0",
         "@modelcontextprotocol/ext-apps": "^1.7.4",
         "@novnc/novnc": "^1.7.0",
@@ -39229,7 +39229,7 @@
     },
     "sdk": {
       "name": "@mcpjam/sdk",
-      "version": "8.9.0",
+      "version": "8.10.0",
       "license": "MIT",
       "dependencies": {
         "@ai-sdk/amazon-bedrock": "^3.0.101",
diff --git a/sdk/CHANGELOG.md b/sdk/CHANGELOG.md
index 75f83f1f9..12c91ccef 100644
--- a/sdk/CHANGELOG.md
+++ b/sdk/CHANGELOG.md
@@ -1,5 +1,17 @@
 # `@mcpjam/sdk` changelog
 
+## 8.10.0
+
+### Minor Changes
+
+- [#5264](https://github.com/MCPJam/inspector/pull/5264) [`07d7090`](https://github.com/MCPJam/inspector/commit/07d709022c9922891145ee14a9637e3f5b7461fd) Thanks [@chelojimenez](https://github.com/chelojimenez)! - Tell agents and scripts when an included operation's usage limit lifts. The SDK adds `describePlatformRefusal` and `platformRefusalHint`, which read a `RATE_LIMITED` refusal's backend code, limit, retry time and whether credits would help. MCP tool errors, CLI errors and in-app agent tool errors now carry that and say when to retry, without suggesting a top-up. Generation copy now says the quota belongs to the organization, not the project, and description proposals no longer claim a generation quota. The insight getters explain `platform_cap_exceeded` and `platform_unavailable`.
+
+- [#5248](https://github.com/MCPJam/inspector/pull/5248) [`b99eed6`](https://github.com/MCPJam/inspector/commit/b99eed6a297508c17f8fa19e78257d09361a9cb5) Thanks [@chelojimenez](https://github.com/chelojimenez)! - Report swarm execution, goal grading, and advisory standard checks separately across the UI and platform API. Show shared run decisions, grading coverage, and session user-value chains while retaining persona-specific findings.
+
+### Patch Changes
+
+- [#5279](https://github.com/MCPJam/inspector/pull/5279) [`e059fd2`](https://github.com/MCPJam/inspector/commit/e059fd2d7bcc2753e2bbcc1ce004a824b009c320) Thanks [@ignaciojimenezr](https://github.com/ignaciojimenezr)! - Stop an empty system prompt from failing every generation. A saved MCPJam client with no system prompt reads as `""`, and `runWithClient` passed it straight to the provider; Anthropic refuses an empty system block with `system: text content blocks must be non-empty`, so every case in the suite failed with a bare "Bad Request". `HostRunner` now treats an empty configured prompt as "none given" and uses its default, the same as it already did for a host snapshot.
+
 ## 8.9.0
 
 ### Minor Changes
diff --git a/sdk/package.json b/sdk/package.json
index a5f8d5a2a..dac8b2190 100644
--- a/sdk/package.json
+++ b/sdk/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@mcpjam/sdk",
-  "version": "8.9.0",
+  "version": "8.10.0",
   "description": "MCP server unit testing, end to end (e2e) testing, and server evals",
   "type": "module",
   "main": "dist/index.js",
