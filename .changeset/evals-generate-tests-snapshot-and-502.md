---
"@mcpjam/inspector": patch
---

Inspector updates:

- Align the generate-tests snapshot with attachment display names so generated suites reference the same names the user sees
- Stop misclassifying upstream 502s as inspector errors in the web error route
