---
"@mcpjam/inspector": patch
---

Swarm screens use the evaluator vocabulary evals already use, and drop the advisory tier swarms no longer have. The goal grading editor, session chips, run scorecard, report panel, running step and Findings now say "evaluator" instead of "check" or "rubric check". The editor says every evaluator must pass for a session to pass, and the judge blurb says its verdict decides the session. Findings no longer renders "advisory friction" rows.
