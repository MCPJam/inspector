---
"@mcpjam/inspector": patch
---

The suite settings Rubric checks row and card now read the deployment's goal-judge policy. On a suite that inherits a policy turning the goal judge off, the row is disabled and the card says the checks are paused, matching what actually runs.
