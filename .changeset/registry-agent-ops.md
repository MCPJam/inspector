---
"@mcpjam/inspector": patch
---

Offer registry directory search and gated install/uninstall on Slack/Discord. Install proposals freeze the endpoint URL and freshness pin so a later click cannot install a row that moved.
