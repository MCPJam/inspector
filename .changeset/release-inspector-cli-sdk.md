---
"@mcpjam/inspector": patch
"@mcpjam/cli": patch
"@mcpjam/sdk": patch
---

Cut a release of `@mcpjam/inspector`, `@mcpjam/cli`, and `@mcpjam/sdk` so the
work already merged into main reaches npm. Version bump only — no code changes.
