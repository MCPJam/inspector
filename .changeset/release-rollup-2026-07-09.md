---
"@mcpjam/inspector": minor
"@mcpjam/sdk": minor
"@mcpjam/widget-react": patch
---

Release the latest Inspector, SDK, and widget runtime updates.

- Add voice messages and audio transcription support in Inspector.
- Add same-origin analytics relay, typed analytics events, and server-side critical event capture.
- Expand hosted Cloud Skills, evals, and swarm execution flows with better error surfacing, pinned-turn support, and run-source scoped CI views.
- Harden computer and harness data-plane security, including loopback-only terminal access, token redaction, JWKS-backed terminal token verification, and service-token bootstrapping.
- Improve host catalog and host compatibility behavior, including image-support mappings, generated fallback catalog support, host update affordances, and safer catalog fallback handling.
- Improve MCP Apps/widget handling with download confirmation, spec/security polish, and widget runtime behavior fixes.
