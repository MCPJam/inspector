---
"@mcpjam/inspector": patch
---

Scorer rows gain a left rail: a step's real number where one exists, an icon and a "when it runs" tooltip where it doesn't, provenance moved to the left edge, and Warn in amber.
