---
"@mcpjam/sdk": patch
"@mcpjam/inspector": patch
---

The OAuth debugger now says which authorization-server metadata URLs it tried and what each returned, instead of "Last error: null" when every one answered with a 4xx.
