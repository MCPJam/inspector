---
"@mcpjam/inspector": minor
"@mcpjam/sdk": minor
"@mcpjam/cli": patch
"@mcpjam/widget-react": patch
---

Release the latest host compatibility, eval export, and dependency updates.

- Add `@mcpjam/sdk/host-compat` for deriving server requirements, evaluating host compatibility, and scanning widget bridge usage.
- Add the Inspector Compatibility destination with conformance gates, host matrices, live observed widget rendering, and caniuse.dev showcase support.
- Add OTLP/OpenInference trace export from eval run detail pages.
- Pin `@modelcontextprotocol/client` to the stable alpha version used by the workspace.
