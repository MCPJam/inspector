---
"@mcpjam/inspector": patch
---

The Evaluate Steps tab can now name a check the way the rest of the product names it, say why a step failed, and stop counting an advisory miss as a failure. Opt-in, so every existing Steps tab is unchanged.
