---
"@mcpjam/inspector": minor
---

Add Inspector public API v1 (`/api/v1`) with live-MCP routes for servers, tools, resources, and prompts. Includes a stable response envelope, internal-to-v1 error code mapping, contract tests, and a session-auth bypass so external clients can use bearer tokens without colliding with the web session middleware.
