---
"@mcpjam/inspector": minor
"@mcpjam/cli": minor
"@mcpjam/sdk": minor
---

Release the latest Inspector, CLI, and SDK updates.

- Add the versioned evaluation contract, comparison gates, corpus locking, and score reporting across all three surfaces.
- Add unified session search, clearer API failures, and safer hosted launch guardrails.
