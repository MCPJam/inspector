{
  "key": "e2b-fleet-snapshot-deadman",
  "name": "MCPJam Backend E2B fleet snapshot silent (deadman)",
  "tier": "WARN",
  "owner": "marcelo",
  "dataset": "mcpjam-backend-prod",
  "notifier": "llm-safety",
  "monitor": {
    "type": "Threshold",
    "columnName": "Snapshots",
    "operator": "Below",
    "threshold": 1,
    "rangeMinutes": 20,
    "intervalMinutes": 5,
    "triggerAfterNPositiveResults": 2,
    "triggerFromNRuns": 2,
    "notifyByGroup": false,
    "alertOnNoData": false,
    "resolvable": true,
    "notifyEveryRun": false
  },
  "aplQuery": [
    "['mcpjam-backend-prod']",
    "| where _sysTime >= ago(20m)",
    "| where tostring(['data.message']) contains '\"event\":\"e2b_fleet_snapshot\"'",
    "| extend msg=tostring(['data.message'])",
    "| summarize Snapshots = count()"
  ],
  "description": [
    "WARN \u2014 owner: marcelo. No e2b_fleet_snapshot in 20 minutes (cron runs every 5).",
    "",
    "TRIAGE: function_execution path 'e2bFleet:emitE2bFleetSnapshot' status; then | where msg contains 'e2b_fleet_snapshot_failed' | project _time, msg.",
    "",
    "Source: e2b_fleet_snapshot (mcpjam-backend convex/e2bFleet.ts, every 5 min from E2B GET /v2/sandboxes). Silent until backend PR #1398 is in PROD."
  ],
  "rationale": [
    "WHY THIS EXISTS: every E2B money monitor above reads this event; if the cron dies or the E2B key is rotated (`e2b_fleet_snapshot_failed` with reason) the fleet is blind again and every Above-monitor looks healthy. DO NOT APPLY until the backend PR is in prod \u2014 before that it is a guaranteed false page.",
    "SOURCE: `e2b_fleet_snapshot`, emitted every 5 min by mcpjam-backend `convex/e2bFleet.ts` (cron 'snapshot e2b fleet') straight from E2B's GET /v2/sandboxes under the shared team key. It is the only view that includes harness boxes, eval sandboxes AND the inspector's desktop browser sessions; `computer_concurrency_snapshot` (projectComputers rows) misses all three. Until that backend PR is in PROD this query returns no rows and the monitor stays silent.",
    "WHY THRESHOLDS ARE PROVISIONAL: no baseline exists yet (fleet was 0 running when this was written). Re-measure after a week: | summarize max(estUsdPerHour), percentile(estUsdPerHour,95) by bin(_time,1d).",
    "ROLLBACK: node ops/axiom-monitors/apply.mjs --delete e2b-fleet-snapshot-deadman --apply, or revert the file and re-apply."
  ]
}