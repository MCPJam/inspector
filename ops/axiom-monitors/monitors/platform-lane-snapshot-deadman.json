{
  "key": "platform-lane-snapshot-deadman",
  "name": "MCPJam platform lane snapshot silent (deadman)",
  "tier": "WARN",
  "owner": "marcelo",
  "dataset": "mcpjam-backend-prod",
  "notifier": "llm-safety",
  "monitor": {
    "type": "Threshold",
    "columnName": "Snapshots",
    "operator": "Below",
    "threshold": 1,
    "rangeMinutes": 30,
    "intervalMinutes": 10,
    "triggerAfterNPositiveResults": 1,
    "triggerFromNRuns": 1,
    "notifyByGroup": false,
    "alertOnNoData": false,
    "resolvable": true,
    "notifyEveryRun": false
  },
  "aplQuery": [
    "['mcpjam-backend-prod']",
    "| where _sysTime >= ago(30m)",
    "| where tostring(['data.message']) contains '\"event\":\"platform_lane_snapshot_completed\"'",
    "| summarize Snapshots = count()"
  ],
  "description": [
    "WARN · No platform_lane_snapshot_completed in 30 minutes (the cron runs every 10). The stale-holds monitor is blind until it returns.",
    "",
    "TRIAGE: Convex logs for 'billing/platformLaneSnapshot:emitPlatformLaneSnapshot'; then | where msg contains 'platform_lane_snapshot_failed' | project _time, msg.",
    "Runbook: ops/axiom-monitors/platform-lanes.md"
  ],
  "rationale": [
    "WHY THIS EXISTS: every Above-monitor on the snapshot reads healthy when the snapshot stops. This is what says the gauge, not the spend, went quiet.",
    "DO NOT APPLY before backend #1470 is in PROD: until then there are no rows and this is a guaranteed false alert.",
    "ROLLBACK: node ops/axiom-monitors/apply.mjs --delete platform-lane-snapshot-deadman --apply."
  ]
}
